
# Plan

## 1. Separate super-admin access

**Role model**
- Add `platform_admin` to `app_role` enum (currently used only for org roles).
- New helper `public.is_platform_admin(uuid)` (SECURITY DEFINER) checking `user_roles`.
- Seed my user as `platform_admin` after I confirm which email is yours.

**Routes (fully isolated from normal auth)**
```
src/routes/admin/login.tsx            → /admin/login  (public, no shell)
src/routes/_platform-admin/route.tsx  → gate: session AND is_platform_admin, else redirect /admin/login
src/routes/_platform-admin/dashboard.tsx
src/routes/_platform-admin/orgs.tsx
src/routes/_platform-admin/subscriptions.tsx
src/routes/_platform-admin/health.tsx
```
- Existing `_authenticated/_shell.admin.tsx` is removed from the normal user shell (regular users lose the "Admin" nav item entirely).
- `/admin/login` uses email+password only (no Google), rejects sign-in if the user is not `platform_admin` and immediately signs them back out with a "Not authorized" message.
- Bearer flow unchanged — same Supabase session, but every admin server fn calls `assertPlatformAdmin(context)` first.

## 2. Admin dashboard content

Server fn `getPlatformOverview` returns:
- **Revenue**: total collected (sum of successful `billing_events` in NGN), current MRR (sum of active subscription plan prices), last-30d revenue, trend sparkline.
- **Growth**: total & new-this-week/month for orgs, users (auth.users count), employees, connected Gmail accounts.
- **Subscriptions**: active vs cancelled vs past_due, broken down by plan.
- **Health**: emails sent today, bounce rate, revoked Gmail connections, domains in `failing` state, recent webhook failures.
- **Recent activity**: last 20 `activity_logs` across all orgs.
- **Orgs table** with search + pagination (name, plan, MRR contribution, users, status, created).

## 3. Public status page (Vercel-style)

- New public route `src/routes/status.tsx` at `/status` (no auth).
- Components probe: API (server fn ping), Database (lightweight `select 1`), Auth (Supabase health), Gmail gateway, Paystack API, DNS resolver route.
- Server route `src/routes/api/public/status.ts` runs the probes and returns `{ components: [{name, status, latency_ms}], incidents: [] }`.
- 90-day uptime grid rendered from a new `platform_status_checks` table (populated by pg_cron every 5 min hitting `/api/public/hooks/status-check`).
- Link from footer.

## 4. Google Gmail OAuth (real setup)

- Call `connector_app_user--connect_client` for `google_mail` — this opens the secure form where **you paste your Google Client ID and Google Client Secret**. Lovable stores them at the workspace level and syncs `GOOGLE_MAIL_APP_USER_CONNECTOR_CLIENT_API_KEY` into this project automatically.
- I do **not** ask you to paste the raw client ID/secret as project env vars — that would be the wrong pattern for this connector.
- Before you paste, add this redirect URI to your Google OAuth client in Google Cloud Console:
  `https://connector-gateway.lovable.dev/api/v1/app-users/oauth2/callback`
- Existing `src/lib/gmail.functions.ts` + `src/routes/_authenticated/_shell.gmail.tsx` already implement the full popup + save + disconnect flow — once the connector client is linked, the "Connect Gmail" button in the employees Gmail page just works.
- After linking, I'll invalidate the `gmail-cfg` query so the amber "setup still needs..." banner disappears.

## 5. Full Supabase export → `SUPABASE_MIGRATION.md`

One self-contained markdown file with ordered SQL blocks you can paste into a fresh Supabase project:

1. Extensions (`pgcrypto`, `pg_net`, `pg_cron` if used).
2. Enums (`app_role` incl. `platform_admin`).
3. Tables (all 24) in dependency order, each with GRANTs.
4. Indexes.
5. Functions (`has_role`, `has_org_role`, `is_org_member`, `is_platform_admin`, `handle_new_user`, `tg_touch_updated_at`, any new ones).
6. Triggers (incl. `on_auth_user_created` → `handle_new_user`).
7. RLS enable + all policies.
8. Storage buckets (`brand-assets`) + storage policies.
9. Seed data (plans, Empyre Homes demo org `11111111-...`, my platform_admin `user_roles` row — placeholder).
10. Auth settings checklist (Site URL, redirect URLs, Google provider notes, HIBP toggle).
11. Secrets checklist (which env vars to set in the new project).
12. Post-migration validation queries.

Generated by reading live schema via `supabase--read_query` on `information_schema` + `pg_policies` + `pg_proc` so nothing is missed.

---

## Technical section

- New migration: `platform_admin` enum value, `is_platform_admin` SECURITY DEFINER function, `platform_status_checks` table (component text, status text, latency_ms int, checked_at timestamptz) with `GRANT SELECT TO anon` for the public grid, admin-only insert.
- Admin server fns live in `src/lib/admin.functions.ts` with `assertPlatformAdmin` helper reused everywhere.
- `_platform-admin/route.tsx` uses `ssr: false` + `beforeLoad` calling `supabase.auth.getUser()` + a `getPlatformAdminStatus` server fn.
- Status probes run in parallel with `Promise.allSettled` and short timeouts (2s each).
- `SUPABASE_MIGRATION.md` written to project root; regenerated any time schema changes.

## Order of execution

1. Migration (enum + is_platform_admin + platform_status_checks + seed placeholder).
2. Admin auth routes + remove admin from user shell.
3. Admin dashboard + supporting server fns.
4. Public /status page + probe endpoint.
5. `connector_app_user--connect_client` for `google_mail` (you paste creds).
6. Generate `SUPABASE_MIGRATION.md` last so it reflects everything above.

## What I need from you

- **Your super-admin email** (the one already signed into this project) so the migration seeds your `platform_admin` role.
- Confirmation that after I open the Google connector form, you'll paste the Client ID + Secret there (not in chat).

Reply "go" and I'll start with step 1.
